The plan is sound and matches the brief. I approved it with four corrections before it builds:

- **Shared rule for tool-call tags:** the "is this a real tool call or a quoted one" rule has to make its decision within a fixed window. As planned, the rule for quoted tags and the rule for unfinished blocks at the end of a stream could contradict each other.
- **Same text on both sides:** the streamed text and the saved text must be sanitized from the same raw text.
- **Reuse the existing pieces:** the second reply path should reuse the terminal-step helpers that already exist rather than copy them.
- **No unfounded samples:** it must not call a test sample "realistic" or "from production" without a source.

I checked the code it cites for the final reply step, and its reading is correct. The leaked message still isn't on the ticket, so it proceeds on the formats the models are known to write.

PLAN-REPLY: Approved. The path map, the ask.ts boundary, the terminal-step reading and the matrix answers are right, and I checked direct-loop-parts.ts:316-345 and direct-agent-strategy.ts:210 myself. Make these corrections as you build. (1) Rows C and E conflict as written. A lone quoted `<tool_call>` near the end of the stream would be kept under C and stripped under E. Use one rule: decide "call or prose" from the opening tag plus the next child tag, within a fixed hold-back window (state the size in code and in formats.md). If the call shape isn't confirmed inside that window, release the text as prose. E applies only to a block whose call shape was already confirmed. Test a quoted tag at the very end of the stream, and a quoted tag followed by more prose than the window holds. (2) Row G: the final `output.text` you batch-sanitize must be the same raw text whose deltas went through the stream. Check this on path (c), where publishLine runs resolveDirectLinks before the ask.ts emit. The fallback line appears only when what's left is empty or whitespace, and it must be identical in the stream and the saved text. Add one test per path (a, b, c) that asserts streamed equals persisted, as well as the pure property test. (3) Fenced and inline code state has to carry across chunks: add a row-A-style test where the fence opens in one chunk and the markup arrives in a later one. (4) In direct-agent-strategy, don't hand-roll the terminal step. Reuse directTerminalNudge (or a shared wording both paths use) and apply it only on the steps where tools are withdrawn, never on every step from 7 onward if more than one step can land there. Make sure native-transport seats also get no callable tools. Keep the OpenAI-style patterns narrow so ordinary prose containing "to=" or angle brackets is never stripped, and add a test for that. In formats.md, label every sample "known vendor format" or "assumed". The leaked message is still unknown, so call nothing "realistic" or "from production" without a source. Check model-dials.ts for whether a GPT seat (gpt-5.6-terra) is live in production and say what you find. The OpenAI-style coverage stays either way.
